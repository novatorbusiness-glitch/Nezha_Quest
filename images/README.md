# images

Static image assets can be placed in this folder.

Quest photo reports uploaded in the app are saved in browser localStorage (client-side), because web apps cannot write selected files directly to the project filesystem.
